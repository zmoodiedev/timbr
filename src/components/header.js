import React from "react";
import Navigation from './nav';

import '../styles/header.css';

function Header() {
    return (
        <header>
            <Navigation />
        </header>
    );
};
export default Header;